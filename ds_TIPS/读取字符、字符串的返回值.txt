1.gets 从键盘读入  
返回值：若成功读取则返回指向字符串的指针，返回 NULL说明读到末尾
2.fgetc 从文件中读入
 fgetc(fp)
返回值：返回读取到的字符，返回EOF代表到了文件尾
3.fgets(fp)
返回值：成功则返回S指针，返回NULL代表都到末尾
4.getc  从键盘读入
返回值：返回读取到的字符，返回EOF代表文件结尾
5.getchar 从键盘读入
返回值：返回读取到的字符，返回EOF代表有错误发生

总之是 读取字符到末尾返回EOF 读取字符串到末尾返回NULL
